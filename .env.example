# ==============================================================================
# Agent RAG MCP Server Configuration
# ==============================================================================

# Gemini API Key (required)
GEMINI_API_KEY=your-gemini-api-key

# ==============================================================================
# Document Store Configuration
# Choose ONE of the following options:
# ==============================================================================

# Option 1: Git Repository (recommended for remote/shared docs)
# ------------------------------------------------------------------------------
# RAG_REPO_URL=https://github.com/your-org/your-repo
# RAG_DOCS_PATH=Docs          # Path to docs directory within repo (default: Docs)
# RAG_BRANCH=main             # Git branch to use (default: main)

# Option 2: Local Directory (for local development)
# ------------------------------------------------------------------------------
# RAG_LOCAL_DOCS_PATH=/path/to/your/docs

# Optional: Custom Store Name
# ------------------------------------------------------------------------------
# RAG_STORE_NAME=my-project   # Auto-generated from URL/path if not set

# ==============================================================================
# Authentication (optional, but recommended for deployment)
# ==============================================================================
# Set a secret token to enable Bearer authentication.
# Clients must send: Authorization: Bearer <your-token>
# If not set, the server runs without authentication (development mode).
# AUTH_TOKEN=your-secret-token-here